// Javascript 복습 및 React 활용 문법 # 3

// 클래스와 상속

/**
 * 기본적으로 JS(ES6)에서의 클래스의 정의 Java와 유사함
 * 클래스 정의 방식
 */
class Person{
  constructor(firstName, lastName) {
    this.firstName = firstName;
    this.lastName = lastName;
  }
}

